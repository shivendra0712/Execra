# ⚠️ TEMPLATE ONLY — fill in real values before kubectl apply
apiVersion: v1
kind: Secret
metadata:
  name: execra-secrets
  namespace: execra
type: Opaque
stringData:
  # Replace these values with your actual API keys before applying.
  # NEVER commit real keys to version control.
  OPENAI_API_KEY: "unused"
  GEMINI_API_KEY: "unused"
  REDIS_PASSWORD: "unused"
